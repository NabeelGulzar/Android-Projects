<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nabee.lab_18sep2017">

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ImageList" />
        <activity android:name=".ImageDetail" />
        <activity android:name=".AboutUs" />
        <activity android:name=".Information" />
        <activity android:name=".Gallery" />
        <activity android:name=".CaptureAndDisplayActivity"></activity>
        <activity android:name=".ForBackgroundActivity" />
        <activity android:name=".PersonalInfoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GetUserInfoActivity"></activity>
    </application>

</manifest>